[Unit]
Description=PowerDNS Authoritative Server (SPHINCS+ Port 5303)
After=network.target mysql.service

[Service]
Type=simple
ExecStart=/usr/sbin/pdns_server --config-dir=/etc/powerdns --config-name=pdns-sphincs
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
