[Unit]
Description=PowerDNS Authoritative Server (Falcon Port 5304)
After=network.target mysql.service

[Service]
Type=simple
ExecStart=/usr/sbin/pdns_server --config-dir=/etc/powerdns --config-name=pdns-falcon
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
